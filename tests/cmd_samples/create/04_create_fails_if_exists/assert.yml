exit_code: 1
stdout:
  - match_type: ORDERED_LINES
    content: |
      # REGEX: ^Created guidance store: /.*/.gydnc$
      # REGEX: ^Created tag_ontology.md: /.*/.gydnc/tag_ontology.md$
      # REGEX: ^Created configuration file: /.*/.gydnc/config.yml$
      # REGEX: ^gydnc initialized successfully in /.*$
      # REGEX: ^$
      # REGEX: ^To activate this configuration for your current session, you can run:$
      # REGEX: ^\s*export GYDNC_CONFIG=".*"$
      # REGEX: ^Consider adding this line to your shell configuration file \(e.g., ~/.zshrc or ~/.bashrc\) for persistent use.$
      Created guidance file: .gydnc/existing-guidance.g6e
      Second create attempt exit code: 1
stderr:
  - match_type: ORDERED_LINES
    content: |
      # REGEX: ^guidance file '.*/.gydnc/existing-guidance.g6e' already exists$
filesystem:
  - path: ".gydnc/existing-guidance.g6e"
    exists: true